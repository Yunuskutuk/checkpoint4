{% extends 'base.html.twig' %}

{% block title %}Hello InscriptionController!{% endblock %}

{% block body %}
    <div class="container">
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
        <h1 class="text-center" style="text-decoration: underline; color: #0a0a0a">Connectez-vous !</h1>
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
        <form class="needs-validation" novalidate method="post" action="{{ path('app_login') }}">
            <div class="champs col-md-4 mb-3">
                <label for="pseudo"><b>Pseudo</b></label>
                <input type="text" class="form-control" id="pseudo" placeholder="PierreGird" required>
                <div class="invalid-feedback">Valeur incorrecte</div>
            </div>
            <div class="champs col-md-4 mb-3">
                <label for="ville"><b>Mot de passe</b></label>
                <input type="text" class="form-control" id="ville" placeholder="Mot de passe" required>
                <div class="invalid-feedback">Valeur incorrecte</div>
            </div>
            <button class="btn btn-primary" type="submit">Connexion</button>
            <a href="{{ path('ajout') }}"><button class="btn btn-primary" type="button">Pas de compte ? C'est par là -></button></a>
        </form>
    </div>
    </div>
    </div>
{% endblock %}
